# تاریخچه تغییرات

## نسخه ۱۴۰۵/۶ — اعتبار ربات، جلسه حضوری اسنپی، پست خودکار دو کاناله

### ربات تلگرام/بله — سیستم اعتبار و تاریخچه
- 🎁 **هفته اول رایگان** بعد از ثبت‌نام + سپس **اعتبار هدیه ۱۰۰,۰۰۰ تومانی** سایت (هر پیام مشاوره هوشمند ۲,۰۰۰ تومان؛ تست/داشبورد/تحلیل همیشه رایگان).
- دکمه‌های جدید کیبورد: **📜 تاریخچه** (پروفایل + تست‌ها + مشاوره‌ها از دیتابیس مرکزی D1) و **💳 اعتبار من** (نوار مصرف گرافیکی، مانده، معادل پیام).
- اتمام اعتبار → بلاک مشاوره با دکمه‌های خرید: 🛒 زرین‌پال (سایت) و 🏦 کارت‌به‌کارت (`PAYMENT_CARD_NUMBER`/`PAYMENT_CARD_NAME`).
- جدول جدید `bot_payments` (شارژهای خریداری‌شده — zarinpal/card/admin) + دستورات `/history` و `/credit`.
- تاریخ‌ها همه‌جا شمسی (`faDate`).

### صفحه «جلسه حضوری اسنپی» — `/inperson`
- مدل اسنپ برای آموزش: دانش‌آموز رزرو می‌دهد → مربی تاییدشده + فضای نزدیک (کلاس خالی مدرسه / فضای کار اشتراکی) هماهنگ می‌شود.
- نقشه SVG سبک ۴ شهر (تهران/شیراز/اصفهان/مشهد) با پین فضاها + لینک مسیریابی بلد و Google Maps + بازه اجاره ساعتی هر فضا.
- تعرفه مربی ۳ سطح (همراه/ارشد/ویژه) + برآورد زنده «مربی + اجاره فضا».
- سه تب: 🎒 رزرو دانش‌آموز | 🧑‍🏫 همکاری مربی | 🏫 اجاره فضای مدرسه.
- API های جدید: `POST/GET/PATCH /api/inperson-booking` و `/api/venue-offer` (POST عمومی با rate-limit، GET/PATCH مشاور/ادمین).
- جداول جدید D1: `inperson_bookings`, `venue_offers` + تب CRM ادمین «جلسات حضوری اسنپی 📍».
- لینک صفحه در منوی همه نقش‌ها (دانش‌آموز/والد/مشاور/دبیر).

### پست خودکار کانال — دو کاناله (پوشه `channel-worker/`)
- Worker جدا `taranom-channel-poster` حالا هر روز (~۱۰:۰۰ تهران) در **تلگرام `@ai_exam_iran` و بله `@taranom_hamdeli_channel`** پست می‌گذارد: سوال روز از بانک کنکور + نکته AI (Llama-3.3-70B).
- endpoint تشخیصی `/bale-check` (وضعیت ربات بله و دسترسی کانال). ⚠️ ربات باید در کانال بله ادمین شود.

### مهاجرت D1 این نسخه
```bash
npx wrangler d1 execute taranom-mehr-db --remote --file=./schema.sql
```
(جداول جدید: `bot_payments`, `inperson_bookings`, `venue_offers` — روی هر دو دیتابیس prod و v3 اعمال شده است.)

---

## نسخه به‌روزرسانی همگام‌سازی پنل مشاور و دانش‌آموز

این نسخه بر پایه نسخه قبلی پروژه ساخته و برای رفع خطاهای دیتابیس، احراز هویت و برنامه‌ریزی درسی به‌روزرسانی شده است.

### اصلاحات اصلی

- افزودن جدول `study_plans` به Cloudflare D1.
- همگام‌سازی برنامه درسی بین پنل مشاور و پنل دانش‌آموز.
- نگهداری برنامه پس از refresh و روی دستگاه‌های مختلف.
- جلوگیری از مخلوط شدن برنامه دانش‌آموزان هنگام تغییر دانش‌آموز فعال.
- اصلاح ساختار تعداد تست‌ها (`qCount` و `totalQ`).
- بازیابی تمام روزهای قابل ویرایش برنامه در پنل مشاور.
- محافظت از پنل مدیریت و فهرست کاربران با نشست و نقش سمت سرور.
- حذف توکن‌های ربات از سورس و انتقال کامل آن‌ها به Cloudflare Secrets.
- غیرفعال‌کردن نمایش OTP در محیط production.
- رفع خطاهای صفحات وبلاگ، دانلود، میز مطالعه و برنامه‌ساز.
- اضافه‌شدن تست‌های API و امنیت و اجرای TypeScript پیش از build.
- به‌روزرسانی وابستگی‌ها تا رسیدن به صفر آسیب‌پذیری شناخته‌شده در `npm audit`.

### مهاجرت ضروری D1

قبل از انتشار این نسخه روی Cloudflare باید `schema.sql` روی دیتابیس production اجرا شود:

```bash
npx wrangler d1 execute taranom-mehr-db --remote --file=./schema.sql
```
