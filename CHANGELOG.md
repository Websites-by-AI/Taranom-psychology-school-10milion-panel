# تاریخچه تغییرات

## نسخه به‌روزرسانی همگام‌سازی پنل مشاور و دانش‌آموز

این نسخه بر پایه نسخه قبلی پروژه ساخته و برای رفع خطاهای دیتابیس، احراز هویت و برنامه‌ریزی درسی به‌روزرسانی شده است.

### اصلاحات اصلی

- افزودن جدول `study_plans` به Cloudflare D1.
- همگام‌سازی برنامه درسی بین پنل مشاور و پنل دانش‌آموز.
- نگهداری برنامه پس از refresh و روی دستگاه‌های مختلف.
- جلوگیری از مخلوط شدن برنامه دانش‌آموزان هنگام تغییر دانش‌آموز فعال.
- اصلاح ساختار تعداد تست‌ها (`qCount` و `totalQ`).
- بازیابی تمام روزهای قابل ویرایش برنامه در پنل مشاور.
- محافظت از پنل مدیریت و فهرست کاربران با نشست و نقش سمت سرور.
- حذف توکن‌های ربات از سورس و انتقال کامل آن‌ها به Cloudflare Secrets.
- غیرفعال‌کردن نمایش OTP در محیط production.
- رفع خطاهای صفحات وبلاگ، دانلود، میز مطالعه و برنامه‌ساز.
- اضافه‌شدن تست‌های API و امنیت و اجرای TypeScript پیش از build.
- به‌روزرسانی وابستگی‌ها تا رسیدن به صفر آسیب‌پذیری شناخته‌شده در `npm audit`.

### مهاجرت ضروری D1

قبل از انتشار این نسخه روی Cloudflare باید `schema.sql` روی دیتابیس production اجرا شود:

```bash
npx wrangler d1 execute taranom-mehr-db --remote --file=./schema.sql
```
